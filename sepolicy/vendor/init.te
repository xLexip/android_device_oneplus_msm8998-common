allow init proc_alarm_boot:file { r_file_perms };
allow init socket_device:sock_file { unlink setattr create };
allow init bt_firmware_file:filesystem getattr;
allow init firmware_file:filesystem getattr;
allow init thermal_engine_prop:file { read write relabelto };

allow init vendor_device_prop:file { read write relabelto };
allow init vendor_param_prop:file { read write relabelto };
allow init vendor_power_prop:file { read write relabelto };

